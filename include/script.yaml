script:
  homeassistant_restart:
    sequence:
      - service: browser_mod.toast
        data:
          duration: 16000
          message: Home Assistant-container wordt herstart...
      - service: shell_command.ha_dockermon_restart_container
